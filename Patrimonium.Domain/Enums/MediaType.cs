namespace Patrimonium.Domain.Enums
{
    public enum MediaType
    {
        Image = 1,      // Imagens (fotos do imóvel, documentos digitalizados, etc.)
        Video = 2,      // Vídeos (tour virtual do imóvel, gravações de inspeção, etc.)
        Document = 3    // Documentos digitalizados (contratos, laudos, etc.)
    }
}